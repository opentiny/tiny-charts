import basicToken from "../basicToken";

const { lineWidthSM } = basicToken;

const GraphTreeChart = () => {
  return {
    lineStyle: {
      width: lineWidthSM,
    }
  };
};

export default GraphTreeChart;