import basicToken from '../basicToken';

const { borderSM } = basicToken;

const FunnelChart = () => {
  return {
    itemStyle: {
      borderWidth: borderSM
    }
  };
};

export default FunnelChart;