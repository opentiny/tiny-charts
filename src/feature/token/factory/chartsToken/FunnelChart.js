function FunnelChart (aliasToken) {
  const { colorLabel, borderWidthNone } = aliasToken;

  return {
    labelColor: colorLabel,
    borderWidthNone: borderWidthNone,
  };
  
};



export default FunnelChart;
