import BaseOption from '../../util/baseOption';

BaseOption.series = [];
BaseOption.tooltip = {
  show: true,
  trigger: 'item',
  formatter: '{b}: {c} ({d}%)',
};
BaseOption.legend = {};

export default BaseOption;
