const option = {
    theme: 'light',
    // gradientColor 表示从仪表盘左到右的渐变色
    // gradientColor.length == 1 时也可以表示单色
    // 注意：splitColor 的优先级高于 gradientColor
    gradientColor: ['#0d9458'],
    pointer: true,
    data: [
        {
            value: 71,
            name: 'Utilization rate'
        }
    ]
};
